{
  "name": "syntax-analyzer",
  "type": "module",
  "scripts": {
    "dev": "tsx src/JackAnalyzer.ts",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "project10": "rm -rf ./submissions && mkdir submissions && echo 'nodejs' > submissions/lang.txt && npm run build && cp dist/JackAnalyzer.js submissions && cd submissions && zip -r project10.zip *"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "memfs": "^4.56.10",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  }
}
